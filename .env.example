# Example environment variables for Inndoor
# Copy this file to .env and fill in secret values for your environment.

# Django settings
SECRET_KEY=replace-me-with-a-secure-key
DJANGO_DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (MySQL) example:
# Format: mysql://<user>:<password>@<host>:<port>/<dbname>
# Replace with your actual values. Example for local MySQL:
DATABASE_URL=mysql://root:localpassword@localhost:3306/inndoor_db

# For external storage (S3/Cloudinary) add keys here when configured
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=

# Email and other service credentials
# SENDGRID_API_KEY=
